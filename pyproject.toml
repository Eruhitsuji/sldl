[project]
name = "sldl-compiler"
version = "1.0.0"
description = "SLDL v1.0.0 stable bilingual official examples release"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sldlc = "sldl_compiler.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
