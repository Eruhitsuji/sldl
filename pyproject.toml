[project]
name = "sldl-compiler"
version = "1.0.5"
description = "SLDL v1.0.5 stable bilingual release with template reference drift checks"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sldlc = "sldl_compiler.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
