[project]
name = "sldl-compiler"
version = "1.0.8"
description = "SLDL v1.0.8 stable bilingual release with schema-template diagnostics hardening"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sldlc = "sldl_compiler.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
