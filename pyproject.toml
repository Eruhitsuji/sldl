[project]
name = "sldl-compiler"
version = "1.0.6"
description = "SLDL v1.0.6 stable bilingual release with manifest consistency hardening"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sldlc = "sldl_compiler.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
