[project]
name = "sldl-compiler"
version = "1.0.1"
description = "SLDL v1.0.1 stable bilingual release with template-schema binding"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
sldlc = "sldl_compiler.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
